{
  "name": "images-service",
  "version": "0.0.1",
  "description": "",
  "author": "Feliciano Ernesto Franco Lux",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "echo \"npm run compile\"",
    "pretest": "npm run compile",
    "posttest": "npm run lint",
    "build": "tsc",
    "start": "node dist/src/main.js",
    "start:dev": "ts-node src/main.ts",
    "install:dev": "rm -rf node_modules && rm -rf package-lock.json && npm install",
    "install:prod": "rm -rf node_modules && rm -rf package-lock.json && npm install --production --no-package-lock",
    "prepare:zip": "npm run install:dev && npm run build && npm run install:prod",
    "make:zip": "npm run prepare:zip && rm -f unimarket-images-service.zip && zip -r unimarket-images-service.zip dist node_modules package.json",
    "update:lambda": "aws lambda update-function-code --function-name unimarket-images-service --zip-file fileb://unimarket-images-service.zip",
    "deploy:lambda": "npm run make:zip && npm run update:lambda",
    "serverless:offline-debug": "rm -rf dist && npm run build && npx serverless offline --debug",
    "serverless:offline": "rm -rf dist && npm run build && npx serverless offline",
    "format": "prettier --write \"src/**/*.ts\"",
    "format:check": "prettier --check \"src/**/*.ts\""
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.812.0",
    "@types/aws-lambda": "^8.10.149",
    "@types/node": "^22.15.18",
    "aws-lambda": "^1.0.7",
    "gts": "^6.0.2",
    "serverless-offline": "^14.4.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@fastify/aws-lambda": "^5.1.4",
    "@fastify/cors": "^11.0.1",
    "@fastify/jwt": "^9.1.0",
    "@fastify/multipart": "^9.0.3",
    "dotenv": "^16.5.0",
    "fastify": "^5.3.3",
    "jsonwebtoken": "^9.0.2"
  }
}
