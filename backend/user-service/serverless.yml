service: user-service

provider:
  name: aws
  runtime: nodejs22.x

functions:
  user:
    handler: dist/main.handler
    timeout: 30
    events:
      - http:
          path: /
          method: get

plugins:
  - serverless-offline
